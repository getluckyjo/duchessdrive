# Never commit rclone credentials or tokens
rclone.conf
service-account*.json
oauth2service.json
*.conf

# Inventory output is account-specific; keep the directory, not the contents
inventory/*
!inventory/.gitkeep

logs/
